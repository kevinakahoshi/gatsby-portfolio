import React from 'react';
import styled from 'styled-components';

const HeroImageElementsStyles = styled.div`
  height: 100%;
  width: 100%;
`;

const HeroImageElements = () => {
  return (
    <HeroImageElementsStyles>

    </HeroImageElementsStyles>
  )
};

export default HeroImageElements;
